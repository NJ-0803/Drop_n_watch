export function Footer() {
  return (
    <footer className="mt-16 border-t border-line/70 pt-6 text-[13px] leading-relaxed text-faint">
      <p>
        Dropwatch reads each store’s public pages when you search and links you straight to the cheapest one. Prices can change at
        checkout, so confirm the size, seller and delivery date before you pay. Not affiliated with any store.
      </p>
      <p className="mt-3 font-serif text-[20px] text-muted">dropwatch.</p>
    </footer>
  );
}
